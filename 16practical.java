//Heap Sort.
public class tryHeapsort {

  static String[] heap;
  static int size;

  //Swap
  static void swap(int i, int j) {
    String temp = heap[i];
    heap[i] = heap[j];
    heap[j] = temp;
  }
  //Minimum heap
}
